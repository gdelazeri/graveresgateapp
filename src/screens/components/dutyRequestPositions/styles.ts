import styled from "styled-components/native";

const Container = styled.View`
  flex-direction: row;
  align-items: center;
  flex-wrap: wrap;
`;

const Item = styled.View`
  margin-top: 4px;
  margin-right: 4px;
`;

export default {
  Container,
  Item,
};
