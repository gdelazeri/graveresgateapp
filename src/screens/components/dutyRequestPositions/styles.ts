import styled from "styled-components/native";

const Container = styled.View`
  flex-direction: row;
  align-items: center;
  margin-top: 4px;
`;

const Item = styled.View`
  margin-right: 4px;
`;

export default {
  Container,
  Item,
};
